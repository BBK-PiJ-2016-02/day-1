double totalBorrowed = Double.parseDouble(
	System.console().readLine("Enter the total amount borrowed for mortgage: ");
);
int yearsToPay = Integer.parseInteger(
	System.console().readLine("Enter the number of years to pay it back: ");
);
double interestRate =  Double.parseDouble(
	System.console().readLine("Enter the interest rate: ");
);

double totalAmountToBePaid() {}
double amountToPayEachYear() {}
double numOfYearsBeforeInterestPaid() {}